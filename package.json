{
  "name": "TenderPro",
  "version": "1.0.0",
  "description": "TenderPro tests",
  "author": "koala",
  "dependencies": {
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-http": "^3.0.0",
    "mocha": "^3.3.0"
  },
  "scripts": {
    "start": "SET NODE_ENV=dev && node server.js",
    "test": "mocha --timeout 20000"
  }
}